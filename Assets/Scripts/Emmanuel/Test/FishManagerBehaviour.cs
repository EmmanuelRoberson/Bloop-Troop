using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FishManagerBehaviour : MonoBehaviour
{
    //list of the fish in the school
    public List<GameObject> schoolAgents;

    //list of each fish's collider
    private List<Collider> schoolAgentColliders;
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
