using UnityEngine;

//Placeholder class that acts as a component to get the components children
namespace Emmanuel
{
    public class FishPositionBehaviour : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
